<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sambal Bledheg Nyah Uban 321</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600;700&display=swap"
      rel="stylesheet"
    />
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    <style>
      body {
        font-family: "Outfit", sans-serif;
      }
      .glass {
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(10px);
      }
      .red-gradient {
        background: linear-gradient(135deg, #7f1d1d 0%, #b91c1c 100%);
      }

      /* FIRE VIDEO OVERLAY STYLES */
      .fire-overlay {
        position: absolute;
        inset: 0;
        z-index: 10;
        overflow: hidden;
        pointer-events: none;
      }

      .fire-video-container {
        position: absolute;
        bottom: 0;
        left: 0;
        width: 100%;
        height: 100%;
        display: flex;
        justify-content: center;
        align-items: flex-end;
        mix-blend-mode: screen; 
        opacity: 0.75; 
        filter: contrast(1.4) brightness(0.7);
        /* Mask to reveal the product in the center-bottom */
        mask-image: radial-gradient(circle at 50% 70%, transparent 10%, black 80%);
        -webkit-mask-image: radial-gradient(circle at 50% 70%, transparent 10%, black 80%);
        pointer-events: none;
      }

      .fire-video {
        width: 100%;
        height: 100%;
        object-fit: cover; /* Cover the whole area */
      }
      
      /* Optional: Add a base glow behind the video */
      .fire-video-container::after {
        content: '';
        position: absolute;
        bottom: 5%;
        left: 20%;
        right: 20%;
        height: 100px;
        background: radial-gradient(ellipse at center, rgba(255, 100, 0, 0.4) 0%, transparent 70%);
        filter: blur(30px);
        z-index: -1;
      }
    </style>

    <% if (locals.setting && locals.setting.google_analytics_id) { %>
    <!-- Google Analytics -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=<%= locals.setting.google_analytics_id %>"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', '<%= locals.setting.google_analytics_id %>');
    </script>
    <% } %>

    <% if (locals.setting && locals.setting.facebook_pixel_id) { %>
    <!-- Facebook Pixel -->
    <script>
    !function(f,b,e,v,n,t,s)
    {if(f.fbq)return;n=f.fbq=function(){n.callMethod?
    n.callMethod.apply(n,arguments):n.queue.push(arguments)};
    if(!f._fbq)f._fbq=n;n.push=n;n.loaded=!0;n.version='2.0';
    n.queue=[];t=b.createElement(e);t.async=!0;
    t.src=v;s=b.getElementsByTagName(e)[0];
    s.parentNode.insertBefore(t,s)}(window, document,'script',
    'https://connect.facebook.net/en_US/fbevents.js');
    fbq('init', '<%= locals.setting.facebook_pixel_id %>');
    fbq('track', 'PageView');
    </script>
    <noscript><img height="1" width="1" style="display:none"
    src="https://www.facebook.com/tr?id=<%= locals.setting.facebook_pixel_id %>&ev=PageView&noscript=1"
    /></noscript>
    <% } %>
  </head>
  <body class="bg-gray-50"></body>
</html>
